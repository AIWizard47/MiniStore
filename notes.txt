but in this i want to store in file not in ram this ok let me explain you again with some example and give me brutal and real review how this idea is start -> This is some type of module that any one can download and import it this is mini and easy to download and import because this help for begginners and who start the project for this any user want to save there file like in json ,data,table that used to store the data and for there small project like calculator and for any other file like for website, small banking system and game this all data save in a file like json and table or in txt file format <- how it is 
here how i want one folder spacial make for user create a there new datatype to store there own choise of data make it in one folder DataType -> for example how i make 'package com.sam.dataTypes;
public class Sam {
    private int id;
    private String name;
    private String number;
    
    public Sam(int id, String name, String number){
        this.id = id;
        this.name = name;
        this.number = number;
    }
    public int getId(){
        return this.id;
    }
    public String getName(){
        return this.name;
    }
    public String getNumber(){
        return this.number;
    }
} '' and then create somthing like this in folder by using python filehandling process and then this python handle the creation of file of this java like Table tb = new Table(id,name,number); then create this file / if i insert the data then filling the json file in same formate also done by python file handling java is for sending input and connecting the file like this -> ''package com.sam.components;
import java.io.BufferedReader;
import java.io.InputStreamReader;
import java.io.OutputStreamWriter;
import java.io.PrintWriter;
// import java.util.*;
public class RunPython {
    public static void run() {
        try {
            // Scanner scanner = new Scanner(System.in);
            // Python executable path
            String pythonPath = "python"; // or full path like C:\\Python311\\python.exe

            // Python script path
            String scriptPath = "./com/sam/pythonScripts/script.py";

            ProcessBuilder pb = new ProcessBuilder(
                    pythonPath,
                    scriptPath
            );

            pb.redirectErrorStream(true);
            Process process = pb.start();

            // 🔹 Send input to Python
            // PrintWriter writer = new PrintWriter(
            //         new OutputStreamWriter(process.getOutputStream())
            // );
            
            // int inputValue = scanner.nextInt();
            // writer.println(inputValue);   // <-- input value
            // writer.flush();
            // writer.close();         // IMPORTANT: close stdin

            // 🔹 Read Python output
            BufferedReader reader = new BufferedReader(
                    new InputStreamReader(process.getInputStream())
            );

            String line;
            while ((line = reader.readLine()) != null) {
                System.out.println(line);
            }

            process.waitFor();

        } catch (Exception e) {
            e.printStackTrace();
        }
    }
} ''like this in python ''# n = int(input("Enter a number: \n"))
# print(f"You entered: {n}")

filename = "./com/Sam/components/Data.java"

file = open(filename, "r").readlines()
open(filename,'w').writelines(file) '' creating of dataType and in other folder json type you understand

This is the my project structure 'MiniStore/
│
├── src/
│   └── com/sam/
│       ├── dataTypes/        ← USER writes Java classes
│       │   └── Sam.java
│       ├── ministore/
│       │   ├── MiniStore.java
│       │   ├── SchemaExtractor.java
│       │   └── PythonBridge.java
│
├── schema/                  ← AUTO-GENERATED
│   └── Sam.schema.json
│
├── data/                    ← AUTO-GENERATED
│   └── Sam.json
│
└── python/
    └── engine.py
''
and wait i providing you full code in next messages